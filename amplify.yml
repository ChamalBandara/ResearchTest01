version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "Building container"
        build:
          commands:
            - docker build -t vuln-detector .
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
    appRoot: .
backend:
  phases:
    build:
      commands:
        - echo "No backend"